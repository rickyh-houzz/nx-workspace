{
  "name": "last-successful-workflow-commit",
  "private": true,
  "version": "1.0.0",
  "description": "github action for getting last successful run commit of a workflow",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx esbuild ./src/index.ts --bundle --outfile=./dist/index.js --platform=node --target=node16 --minify"
  },
  "author": "Ricky Hsieh",
  "license": "UNLICENSED",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.15.11"
  }
}
