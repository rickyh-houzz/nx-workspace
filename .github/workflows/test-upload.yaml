name: upload
on:
  pull_request:

jobs:
  upload:
    runs-on: ubuntu-latest
    outputs:
      manifest_commit_sha: ${{ 123 }}
    steps:
      - name: jdjdjd
        run: |
          exit 0
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: '14'
      - name: create file
        run: date +%s > report.txt
  get:
    runs-on: ubuntu-latest
    if: ${{ 1 == 1 }}
    needs: upload
    steps:
      - name: Download
        run: |
          echo "${{ needs.upload.manifest_commit_sha || 0 }}"

